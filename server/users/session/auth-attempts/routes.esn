'use strict';
import RouteFactory from './../../../common/route-factory';
import Controller from './controller';
let routes = (new RouteFactory())
    .discoverDefaultRoutes('auth-attempts', Controller)
    .doneConfiguring();
export default routes;
