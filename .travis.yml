language: node_js
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
node_js:
  - "5"
services:
  - mongodb
before_script:
  - npm run server:build:clean
  - npm run server:setup
  - mkdir ./test/artifacts
  - mkdir ./logs
