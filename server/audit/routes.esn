'use strict';
import RouteFactory from './../common/route-factory';
import Controller from './controller';
let router = (new RouteFactory())
    .discoverDefaultRoutes('audit', Controller)
    .doneConfiguring();
export default router;
